<form
        method="post"
        action="{{ path('app_client_case_delete', {'id': clientCase.id}) }}"
>
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ clientCase.id) }}">
    <button class="btn btn-danger">
        Supprimer
    </button>
</form>