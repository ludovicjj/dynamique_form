<form
        method="post"
        action="{{ path('app_partner_contact_delete', {'id': contact.id}) }}"
>
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contact.id) }}">
    <button class="btn btn-danger">
        Supprimer
    </button>
</form>