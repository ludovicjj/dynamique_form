<div {{ attributes }}>
    <div class="col-12">
        <div
                class="d-flex justify-content-center align-items-center bg-danger text-white mb-4 m-auto rounded-circle fs-3"
                style="width: 45px; height: 45px;"
        >
            <twig:Core:Icon provider="mage" name="exclamation-triangle"/>
        </div>

        <h5 class="text-center mb-4">Attention !</h5>

        <p class="text-center mb-4">
            Vous êtes sur le point de supprimer l'affaire
            <span class="fw-bold">{{ clientCase.reference }}</span>.
            Toutes les données et associations avec cette affaire seront perdu.
        </p>

        <div class="d-flex flex-column">
            <button
                    class="btn btn-danger mb-4"
                    data-action="live#action"
                    data-live-action-param="delete"
                    data-loading="addAttribute(disabled)"
                    {{ loading ? 'disabled' : '' }}
            >
                Supprimer
            </button>
            <button
                    class="btn btn-secondary"
                    data-action="live#emit"
                    data-live-event-param="name(ClientCase:ClientCaseSearch)|reset"
                    data-loading="addAttribute(disabled)"
                    {{ loading ? 'disabled' : '' }}
            >
                Annuler
            </button>
        </div>

    </div>
</div>